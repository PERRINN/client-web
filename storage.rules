service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read; // 👈 allows anyone to read (no auth required)
      allow write: if request.auth != null; // 👈 only authenticated users can write
    }
  }
}
